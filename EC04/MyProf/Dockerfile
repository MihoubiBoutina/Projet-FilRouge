# 1. On force l'ajout du Dockerfile modifié
git add EC04/MyProf/Dockerfile

# 2. On valide le commit
git commit -m "Fix: Limitation de memoire desactivee pour Composer"

# 3. On envoie sur GitHub
git push origin main